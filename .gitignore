node_modules/
lib/
lib-test/
lib-e2e/
*.log
.DS_Store
# Live E2E credential file (Issue #7) — real values, NEVER committed.
.env.e2e.local
# Local verification homes / pnpm scratch (Issue #8) — never committed.
.verify-home/
.pnpm-store/
# npm pack build artifact (Issue #8) — regenerated by `npm pack`, not source.
*.tgz
